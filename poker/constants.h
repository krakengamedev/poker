#pragma once

const int SCREENWIDTH = 500;
const int SCREENHEIGHT = 500;



/*
* <iomanip>
 cout << setw(x) sets the width to x characters for the next cout
 cout << fixed use decimal notation
 setprecision(x) how many decimals are placed
 showpoint print the decimal no matter what
 getline(cin, varToSaveTo) is a cin that grabs all the things so that even whitespace is captured
 cin.get(charVar) gets a single char
 cin.ignore() if you use cin and get() in the same program

 string
 string.length()
 string + operator concatenation

 cmath

 stdlib
 rand()
 srand() (seeds the rand engine)

 random_device myEngine
 uniform_int_distribution<int> randomInt(0,100);
 int number = randomInt(myEngine)
*/
